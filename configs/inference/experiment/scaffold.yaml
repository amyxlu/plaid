# @package _global_

# see configs/inference/full.yaml for the full pipeline configuration.
# this sets up configs for an example motif scaffold pipeline experiment.

defaults:
  - _self_
  - override /sample: motif_scaffold 

use_designability_filter: False
run_cross_consistency: False 
run_self_consistency: False 
run_analysis: False 
